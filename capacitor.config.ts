import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "edu.vku.facilityinspect",
  appName: "VKU Ki\u1ec3m K\u00ea",
  webDir: "dist",
  server: {
    androidScheme: "https"
  }
};

export default config;
